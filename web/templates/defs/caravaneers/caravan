<p>
    A line in a near-to-far direction
    which contains either
    1
    $piece[0],
    or 2 or more
    $piece[0]
    in mutually abutting spaces,
    and
    whose furthest $piece[0] is not in
    a $keywords("marked-space")
</p>
